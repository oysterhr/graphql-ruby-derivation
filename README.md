# graphql-ruby-derivation
A Ruby gem providing composable, independently requireable utilities for working with `graphql-ruby` schemas. Argument and field derivation from multiple source types, with optional Rails and ActiveRecord integration.
